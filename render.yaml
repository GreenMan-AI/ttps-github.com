services:
  - type: web
    name: wave-platform
    env: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: wave-data
      mountPath: /app/data
      sizeGB: 1
